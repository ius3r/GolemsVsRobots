using UnityEngine;

/// <summary>
/// Marker component for "The Core" (main objective).
/// Add this to the Core GameObject. Give it a TeamAffiliation of Robots so robots never target it.
/// </summary>
public sealed class CoreTarget : MonoBehaviour
{
}
